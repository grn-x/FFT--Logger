<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:tools="http://schemas.android.com/tools"
        tools:showIn="navigation_view">

    <group android:checkableBehavior="single">
        <item
                android:id="@+id/nav_home"
                android:icon="@drawable/ic_menu_start_pic"
                android:title="@string/menu_start" />
        <item
                android:id="@+id/nav_gallery"
                android:icon="@drawable/ic_menu_charts_pic"
                android:title="@string/menu_charts" />
        <item
                android:id="@+id/nav_slideshow"
                android:icon="@drawable/ic_menu_results_pic"
                android:title="@string/menu_results" />
    </group>
</menu>